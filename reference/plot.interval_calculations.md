# Produce a plot for a interval_calculations object

Produce a plot for a interval_calculations object

## Usage

``` r
# S3 method for class 'interval_calculations'
plot(x, ...)
```

## Arguments

- x:

  `interval_calculations` object containing the summary calculated by
  `interval`

- ...:

  Arguments to be passed to methods

## Value

object of class `ggplot` that contains the graphic

## Author

Trent Henderson
